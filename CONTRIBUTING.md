# 贡献指南

[English](CONTRIBUTING.en.md) | 中文

欢迎参与 **Ascend** —— 自下向上构造因果世界的 AI 原生模拟平台。

本文档面向首次接触本项目的贡献者，帮助你了解项目定位、设计原则，以及开发、测试、提交与发布的约定。

## 项目定位

Ascend 兼具双重身份：

- **研究平台**：可复现、可干预、可追溯的因果世界，产出时空序列用于世界模型训练
- **游戏**：玩家作为世界个体，通过基因改造影响族群演化；NPC 由 AI 动态驱动

## 设计原则（必须遵守）

请在动手前确认，你的改动不违反以下原则：

### 深模块低耦合

模块对外只暴露**小而深的接口**，把内部复杂度藏在里面，不泄露实现细节。调用方只需理解接口契约，无需了解内部机制。

### 无补丁式代码

不写绕过问题的 hack。遇到 bug 时，请先定位**根因**、系统性解决问题。补丁式代码会掩盖问题、累积技术债。

### 前后端分离

后端负责世界与智能体逻辑，前端负责渲染、UI、输入、音频，通过明确接口分离职责。现有实现使用 JSON over TCP；完全重构中的绑定与传输方式以[整体架构](docs/整体架构.md)的确认结果为准，目前尚未选定。

### 数据与算法拆分

数据是数据、算法是算法，各自独立演进、互不耦合。改动数据定义不应波及相关算法，反之亦然。

### 不承诺向后兼容

现阶段无历史包袱，重构优先，允许直接修改接口。不要为了兼容旧接口而保留冗余设计。

此原则针对开发接口和重构，不替代新架构的游戏存档兼容目标；模组更新后的续玩策略见[模组管理](docs/游戏平台/模组管理/设计.md)。

## 开发流程

世界与游戏架构正在重新设计，文档结构与逐模块讨论顺序见[世界](docs/世界/综述.md)、[智能体](docs/智能体/综述.md)、[游戏平台](docs/游戏平台/综述.md)与[研究平台](docs/研究平台/综述.md)。新设计中的待讨论议题不构成实施要求；[旧设计归档](docs/归档/README.md)仅供追溯。下列数据、测试和发布说明继续适用于现有实现，后续按已确认的设计更新。

开发新模块时，请遵循以下统一顺序：

1. **明确需求**：先想清楚要解决什么问题
2. **定义接口**：确定模块对外契约
3. **写测试**：先写测试定义预期行为
4. **再写代码**：实现直至测试通过

## 文档维护

本节是工程文档结构与写作规范的维护入口，适用于[整体架构](docs/整体架构.md)及世界、智能体、游戏平台、研究平台四分区。作者撰写的[研究综述](docs/研究理论/研究综述.md)是研究最高指导依据，原文保留；其他旧研究材料已归档，具体研究约束与验证方案须重新讨论。

### 文档职责与唯一来源

| 文档 | 负责内容 |
| --- | --- |
| `docs/整体架构.md` | 跨分区职责、库与平台边界、依赖及装配原则；细节链接到所属模块 |
| 父模块 `综述.md` | 模块职责、子模块导航、依赖和设计进度；不复制叶模块的完整规则 |
| 叶模块 `设计.md` | 目标、概念、行为规则、接口语义、配置、取舍、验收及待决问题 |
| 叶模块 `实现.md` | 实现方案、实际落地范围、代码与构建入口、测试和性能证据 |
| 研究综述 | 研究最高指导依据；后续研究约束、形式化与协议须据此讨论，旧契约不具有优先权 |
| 归档 | 历史材料，不作为当前实施要求 |

- 每条规则由一个文档负责维护；其他文档只摘要并链接。按职责确定来源，不以文件更高层、更新或标题更强势自动覆盖其他范围的规则。
- 世界目录描述通用能力与机制；玩法、模组续玩策略属于游戏平台，实验锁定和研究验证要求属于研究平台，认知实现属于智能体。
- 研究综述与工程范围不一致时，在研究平台记录对应关系及待解决差异，不通过改写综述或复制旧条款消除表面冲突。旧研究方案仅供追溯，重新采用须明确讨论结论。
- 保留现有模块层级：父模块用 `综述.md`，叶模块用 `设计.md` 与 `实现.md`。尚未启动的议题可以只列在父综述中，不批量创建空目录。

### 状态必须分开表达

- **设计状态**：`待讨论`（只有议题）、`讨论中`（部分规则明确）、`基础确定`（职责与核心行为可供依赖）、`可实施`（指定范围的接口、边界和验收充分）。暂停推进作为附注，不冒充设计已完成。
- **决策状态**：`已确认`可作为实施依据；`暂定`可据此推进但要写复核条件；`候选`仅供比较，不构成要求。待决问题集中列出。
- **实现状态**：`未实施`、`部分实施`、`已实现`；已实现必须说明验证范围和证据，不能只凭同名旧代码认定新设计已落地。
- 文档提交、移动、格式整理、讨论转向或未获反对，都不构成候选获批。编辑者不得自行把推荐升级为已确认，改变行为规则需明确讨论结论。

文档头至少标明相应状态、适用范围和导航。设计文档补充已确定范围与尚未覆盖范围；实现文档链接设计依据，不重复维护设计状态。父综述中的进度摘要须与子文档一致。

### 叶模块模板

`设计.md` 按下列顺序组织，允许合并简短章节；未开始讨论时保留简洁骨架，不为填模板虚构内容：

1. **目标与职责**：问题、使用场景、与相邻模块的分工。
2. **概念与术语**：对象、状态与名称含义。
3. **已确认规则**：按主题定义一次；暂定内容独立标注并说明复核条件。
4. **对外契约**：输入、输出、前提、结果、生效时机、失败行为；语义先于函数签名。
5. **配置与边界情况**：单位、范围、默认值、可变性及适用异常情形。
6. **取舍与候选方案**：理由、代价、备选及尚未选择的部分。
7. **验收标准**：可检查的条件、操作和预期；候选验收与已确认规则分开。
8. **待决问题**：影响范围、是否阻塞实施、由哪个模块或阶段解决。

`实现.md` 使用：**设计依据 → 实现方案 → 当前落地情况 → 代码与构建入口 → 验证与性能**。方案中的未来时、当前事实和未完成项分开；尚无代码或测量时明确写无，不杜撰文件、命令或结果。

### 内容质量与维护流程

- 重要规则可使用模块内稳定编号，供接口与验收引用；不要求所有段落编号。示例数值明确标记，不能被读作默认配置。
- 将“确定性好”“高性能”等目标改写为条件明确的行为或测量目标；说明适用运行组合。普通工程验收不自动升级为研究证明义务。
- 讨论中先记录候选；确认后更新唯一来源；阶段收束时清理重复、过期候选与聊天纠正语句，保留必要的取舍理由。普通历史由 Git 保存，重大决策才考虑独立记录。
- 实现变更同步更新落地范围及验证证据；设计变更检查引用方。搬移文档时修复相对链接，并更新上级目录和需要的中英文入口。
- 纯文档修改至少检查差异空白、本地链接与章节锚点、模块导航和状态一致性。整理中承诺保留的研究／归档材料应核对内容未变。格式检查不能代替语义核对；不因文档改动运行无关代码测试。

## 世界内容数据（JSON）

地形、群系、气候、天气、世界生成参数等"可调内容"以 JSON 存在 `data/`
（`terrain.json`/`climate.json`/`biome.json`/`weather.json`/`world.json`），
**改内容只改数据文件、不用改代码**。约定：

- 键为命名空间 id（如 `ascend:grassland`）；`value` 显式声明、唯一且连续
  0..n-1，**已发布值不可改、只追加**（改序号会破坏存档/缓存）
- 显示名只存 `label_key`（如 `terrain.grassland`），文案在 `lang/*.json`
  （中英两文件同步，有对账测试）
- **豁免（仅适用于键值型注册表）**：`weather.json` 的 `features` 为
  "效果条目"注册表——运行时以命名空间 id 的 local 部分（`type_name`，
  如 `cold_snap`）为标识，不参与 `value` 连续编码，也不含 `label_key`
  （无界面显示名；文案走事件键，如 `weather.intensity`）
- 加内容 = 数据文件加一项（`value` 追加 + `label_key`）；import 期校验 +
  测试兜底契约；发行自动携带 `data/` 与 `lang/`

## 测试

以下研究声明管线及 WC 条款引用用于维护现有实现，对应[归档世界契约](docs/归档/研究理论/世界契约.md)。旧工具的通过结果不确立新架构的研究要求或完成状态。

### 后端（Python / pytest）

每次代码变更后，请运行受影响的单元测试（日常开发不必跑全量）：

```bash
.venv/bin/python -m pytest --testmon -n 4 -q
```

- `-n 4` 为保守的并行数：若用 `-n auto`，会按本机核数开满 worker，可能导致世界生成时内存/CPU 双爆、进程卡死。该命令仅用于单元测试
- 集成测试必须串行（端口/子进程冲突）：`.venv/bin/python -m pytest testbench/integration -v`
- 全量测试无需本地手动执行，发布前由 CI 的 `test` job 自动运行

### 前端（GDScript / GUT）

```bash
cd miskhak/client && ./run_tests.sh unit
```

GUT 不随仓库分发（`miskhak/client/addons/gut` 只需在本地安装），因此前端测试仅在本地运行。

### 研究声明管线

研究方程的唯一事实源是 `olam/modules/` 的模块声明（六种声明 +
实现绑定），研究侧投影为 `kheker/equations/equations.json`。修改任何方程、
参数、节点声明或 `data/*.json` 后，必须重新生成并提交生成物，否则 CI 漂移
门禁会失败：

```bash
.venv/bin/python kheker/equations/export_registry.py          # 世界声明 → equations.json
.venv/bin/python kheker/equations/gen_lean.py                 # equations.json → Lean 数据段
.venv/bin/python kheker/equations/export_impl_digests.py      # 实现摘要表（打包身份）
.venv/bin/python kheker/equations/export_frozen_tables.py     # 预计算表（仅表规格变更时运行）
.venv/bin/python kheker/equations/verify_equations.py --fast  # 全链对拍（V0–V4）
.venv/bin/python kheker/equations/graph_check.py              # 图健康巡检（G0–G7）
```

`equations.json`、`GenDeclarationData.lean`、`impl_digests.json` 与
`olam/kernel/frozen_tables.py` 均为生成物，禁止手改。

**干预执行器**（见《世界契约》WC-6）：研究者干预经 `olam/protocols/timeline.py`
登记为计划与逐帧记录，在求值点替换生成；时间线校验以编译程序为唯一事实源
（槽位存在且由机制写入、权限/值域/实例域、参数须被已接线机制消费），改动后
由 `testbench/world/test_timeline.py` 与接线漂移测试盯防。

**验收 runner**：改声明/引擎后跑
`.venv/bin/python kheker/acceptance/run_acceptance.py`（C0–C2/W0–W7/I0–I1/L3，任一
判据失败即红）；改声明后还需 `run_acceptance.py --check` 巡检 Lean UnrolledDag 实例（含机器证明的 `WellFormed`，声明漂移即失败）。

**研究记录**（见《世界契约》WC-10）：研究日志与玩法事件分库——新增机制自动
被记录；**不要把记录字段写进事件载荷**（有门禁测试）；求值记录走
`olam/protocols/records.py` 的 `TraceLog`，引擎在挂载记录时逐机制捕获。

**完整存档**（见《世界契约》WC-7.5、WC-8）：新增"无法由世界设置重算"的运行时
状态（研究者施加的量、随机制演化的标记）时，必须同步 `state.json.enc` 载荷与
恢复路径（`save/serializer.py` + 该子系统的 `persist_*` / `restore_*`），并补
W4 双跑一致断言；可重算的解析量**不得**落盘。改动状态载荷格式时递增
`STATE_VERSION`（旧档即不可读，不写迁移）。

## 提交约定

请遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范，并使用中文撰写提交描述。

## 贡献许可

- 提交 Pull Request 前，请阅读并同意 [CLA.md](CLA.md)（贡献者许可协议）——授权维护者对包含你贡献的软件进行商业化许可与分发
- 提交 PR 时请在模板中勾选"贡献者确认"，即视为同意 CLA
- CI 会通过 [CLA Check](.github/workflows/cla.yml) 自动核验，未同意的 PR 标记为不合并
- 若贡献含第三方材料（代码、库、资源等），务必在 PR 中注明来源与适用许可证
- 项目采用 [CC BY-NC-SA 4.0](LICENSE) 协议，你的贡献亦在该协议下公开发布

## 发布

- 版本号单一来源：`build/version/`（`core.txt` 共享核心 + `miskhak.txt` /
  `kheker.txt` 产品版本 = 核心版本 + 产品序号；`build/ci/check_version.sh` 对账）
- 推送 `research-v*` 标签会触发 CI 自动发研究包；此操作通常由维护者执行：

```bash
git push origin main
git tag research-v<版本> && git push origin research-v<版本>
```

- 本地打包：`bash build/package.sh miskhak|kheker [linux|windows|all]`（详见 `build/README.md`）
- 游戏包（含闭源资产）走私有流程，不在本仓库 CI
