# 贡献指南

[English](CONTRIBUTING.en.md) | 中文

欢迎参与 **Ascend** —— 自下向上构造因果世界的 AI 原生模拟平台。

本文档面向首次接触本项目的贡献者，帮助你了解项目定位、设计原则，以及开发、测试、提交与发布的约定。

## 项目定位

Ascend 兼具双重身份：

- **研究平台**：可复现、可干预、可追溯的因果世界，产出时空序列用于世界模型训练
- **游戏**：玩家作为世界个体，通过基因改造影响族群演化；NPC 由 AI 动态驱动

## 设计原则（必须遵守）

请在动手前确认，你的改动不违反以下原则：

### 深模块低耦合

模块对外只暴露**小而深的接口**，把内部复杂度藏在里面，不泄露实现细节。调用方只需理解接口契约，无需了解内部机制。

### 无补丁式代码

不写绕过问题的 hack。遇到 bug 时，请先定位**根因**、系统性解决问题。补丁式代码会掩盖问题、累积技术债。

### 前后端分离

世界与智能体实现同客户端和平台职责分离；具体绑定与传输方式以[整体架构](docs/整体架构.md)及所属模块的已确认设计为准。

### 数据与算法拆分

数据是数据、算法是算法，各自独立演进、互不耦合。改动数据定义不应波及相关算法，反之亦然。

### 不承诺向后兼容

新架构优先明确职责与契约；开发接口按已确认设计演进，不增加未定义的兼容要求。

此原则针对开发接口和重构，不替代新架构的游戏存档兼容目标；模组更新后的续玩策略见[模组管理](docs/游戏平台/模组管理/设计.md)。

## 开发流程

项目按[整体架构](docs/整体架构.md)推进。逐模块计划与进度见[世界](docs/世界/综述.md)、[智能体](docs/智能体/综述.md)、[游戏平台](docs/游戏平台/综述.md)与[研究平台](docs/研究平台/综述.md)。待讨论议题不是实施要求。

开发新模块时，请遵循以下统一顺序：

1. **明确需求**：先想清楚要解决什么问题
2. **定义接口**：确定模块对外契约
3. **写测试**：先写测试定义预期行为
4. **再写代码**：实现直至测试通过

## 文档维护

本节是工程文档结构与写作规范的维护入口，适用于[整体架构](docs/整体架构.md)及世界、智能体、游戏平台、研究平台四分区。作者撰写的[研究综述](docs/研究理论/研究综述.md)是研究最高指导依据；具体研究约束与验证方案由研究平台记录。

### 文档职责与唯一来源

| 文档 | 负责内容 |
| --- | --- |
| `docs/整体架构.md` | 跨分区职责、库与平台边界、依赖及装配原则；细节链接到所属模块 |
| 父模块 `综述.md` | 模块职责、子模块导航、依赖和设计进度；不复制叶模块的完整规则 |
| 叶模块 `设计.md` | 目标、概念、行为规则、接口语义、配置、取舍、验收及待决问题 |
| 叶模块 `实现.md` | 实现方案、实际落地范围、代码与构建入口、测试和性能证据 |
| 研究综述 | 研究最高指导依据；研究约束、形式化与协议按综述讨论并记录 |

- 每条规则由一个文档负责维护；其他文档只摘要并链接。按职责确定来源，不以文件更高层、更新或标题更强势自动覆盖其他范围的规则。
- 世界目录描述通用能力与机制；玩法、模组续玩策略属于游戏平台，实验锁定和研究验证要求属于研究平台，认知实现属于智能体。
- 研究综述与工程范围不一致时，在研究平台记录对应关系及待解决差异，并明确后续讨论与验证范围。
- 保留现有模块层级：父模块用 `综述.md`，叶模块用 `设计.md` 与 `实现.md`。尚未启动的议题可以只列在父综述中，不批量创建空目录。

### 状态必须分开表达

- **设计状态**：`待讨论`（只有议题）、`讨论中`（部分规则明确）、`基础确定`（职责与核心行为可供依赖）、`可实施`（指定范围的接口、边界和验收充分）。暂停推进作为附注，不冒充设计已完成。
- **决策状态**：`已确认`可作为实施依据；`暂定`可据此推进但要写复核条件；`候选`仅供比较，不构成要求。待决问题集中列出。
- **实现状态**：`未实施`、`部分实施`、`已实现`；已实现必须说明验证范围和证据。
- 文档提交、移动、格式整理、讨论转向或未获反对，都不构成候选获批。编辑者不得自行把推荐升级为已确认，改变行为规则需明确讨论结论。

文档头至少标明相应状态、适用范围和导航。设计文档补充已确定范围与尚未覆盖范围；实现文档链接设计依据，不重复维护设计状态。父综述中的进度摘要须与子文档一致。

### 叶模块模板

`设计.md` 按下列顺序组织，允许合并简短章节；未开始讨论时保留简洁骨架，不为填模板虚构内容：

1. **目标与职责**：问题、使用场景、与相邻模块的分工。
2. **概念与术语**：对象、状态与名称含义。
3. **已确认规则**：按主题定义一次；暂定内容独立标注并说明复核条件。
4. **对外契约**：输入、输出、前提、结果、生效时机、失败行为；语义先于函数签名。
5. **配置与边界情况**：单位、范围、默认值、可变性及适用异常情形。
6. **取舍与候选方案**：理由、代价、备选及尚未选择的部分。
7. **验收标准**：可检查的条件、操作和预期；候选验收与已确认规则分开。
8. **待决问题**：影响范围、是否阻塞实施、由哪个模块或阶段解决。

`实现.md` 使用：**设计依据 → 实现方案 → 当前落地情况 → 代码与构建入口 → 验证与性能**。方案中的未来时、当前事实和未完成项分开；尚无代码或测量时明确写无，不杜撰文件、命令或结果。

### 内容质量与维护流程

- 重要规则可使用模块内稳定编号，供接口与验收引用；不要求所有段落编号。示例数值明确标记，不能被读作默认配置。
- 将“确定性好”“高性能”等目标改写为条件明确的行为或测量目标；说明适用运行组合。普通工程验收不自动升级为研究证明义务。
- 讨论中先记录候选；确认后更新唯一来源；阶段收束时清理重复、过期候选与聊天纠正语句，保留必要的取舍理由。普通历史由 Git 保存，重大决策才考虑独立记录。
- 实现变更同步更新落地范围及验证证据；设计变更检查引用方。搬移文档时修复相对链接，并更新上级目录和需要的中英文入口。
- 纯文档修改至少检查差异空白、本地链接与章节锚点、模块导航和状态一致性。格式检查不能代替语义核对；不因文档改动运行无关代码测试。

## 测试

当前自动化测试覆盖位于 `kheker/native/` 的 C++17 无界面声明引擎，使用 CMake 3.20 与 CTest，不依赖 Qt。修改后在仓库根目录执行：

```bash
cmake -S kheker/native -B build/work/native -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=ON
cmake --build build/work/native --config Debug --parallel 4
ctest --test-dir build/work/native --build-config Debug --output-on-failure
```

测试放在 `testbench/native/`，构建产物放在已忽略的 `build/work/`。[Native Engine CI](.github/workflows/native_engine.yml)在 Linux、Windows、macOS 上构建同一测试集；本地验证记录见[实现文档](docs/研究平台/实验环境与声明接入/实现.md#5-验证与性能)。

## 提交约定

请遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范，并使用中文撰写提交描述。

## 贡献许可

- 提交 Pull Request 前，请阅读并同意 [CLA.md](CLA.md)（贡献者许可协议）——授权维护者对包含你贡献的软件进行商业化许可与分发
- 提交 PR 时请在模板中勾选"贡献者确认"，即视为同意 CLA
- CI 会通过 [CLA Check](.github/workflows/cla.yml) 自动核验，未同意的 PR 标记为不合并
- 若贡献含第三方材料（代码、库、资源等），务必在 PR 中注明来源与适用许可证
- 项目采用 [CC BY-NC-SA 4.0](LICENSE) 协议，你的贡献亦在该协议下公开发布

## 构建与发布

新架构的产品打包、版本方案和发布流程尚未确定；待相关设计确认后再建立对应流程。
