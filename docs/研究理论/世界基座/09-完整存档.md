# 世界基座 09：完整存档

> **历史文档（P2 重构前，2026-09）**：本文描述的旧实现（`causal/`、`runtime/`、`num/`、`fate/`）已在 P2-3c 删除，仅存于 git 历史。
> 现行实现见[世界架构 00：总纲](../世界架构/00-总纲.md)与[01：重构计划](../世界架构/01-重构计划.md)（`backend/ascend/world/`）。

> **实现注记**：本文记录当前实现的做法与背景，不具规范效力。世界语义以《[世界契约](../世界契约.md)》条款为准；若本文与契约冲突，以契约为准。

> **B2 更新（2026-09-15，issue #48 步骤 2）**：天气侧干预载荷已改为 `{"plan": [...], "records": [...]}`（计划 = 实验上下文，记录 = 只追加的已发生前缀），`STATE_VERSION = 2`。本文以下载荷示例与 `InterventionTable.commit` 相关描述为重构前做法，仅作历史实现注记。

> **C2 更新（2026-09-15）**：地形状态随 chunk 持久化，新增每 chunk 积分游标 `integrated_through`（`chunk_tiles` 列）；状态变更置脏后随存档脉搏回写，读档按游标续算、不重放历史。本文"结算日 `settled_day`"相关描述为重构前做法。

> 记号按[工程符号体系](../工程符号体系.md) §2–§4 与
> [第一阶段实施定义](../第一阶段实施定义.md) §3、[世界验收协议](04-世界验收协议.md) §3.5。
> 工程侧入口是 `backend/ascend/save/`（状态载荷）与各子系统的
> `persist_*` / `restore_*` 接口。

## 1. 定位

本篇对应 issue #46 的 P4 交付：**完整存档**。第一阶段要求世界状态
$\mathcal W_t$ 必须包含一切会改变未来演化的信息（实施定义 §3.1），
删除缓存并由 $\mathcal W_t$ 重算后轨迹必须一致。W4 验收就是这个断言。

P2 交付干预执行器后，世界多了一类**声明之外**的状态：研究者施加的干预
（替换规格 × 时长）与强制注入的特征核。它们不由 seed 派生、不能重算，
却真实改变未来。P4 之前这些状态不落盘——读档后干预消失、强制天气消失，
W4 实测失败。

**一句话边界**：存档只存"删掉它未来就会变"的东西。

| 分量 | 处置 | 依据 |
| --- | --- | --- |
| 时钟时间/速度/暂停 | 落盘 | 不可重算 |
| 玩家实体与位置 | 落盘 | 实体状态不可重算 |
| 干预表（生效记录） | 落盘 | 研究者施加的替换，不在声明内 |
| 注入的特征核 | 落盘 | 研究者施加的运行时核，非 seed 派生 |
| 自然特征核时间线 | 重建 | 由 seed + 块坐标 + 段索引确定性派生 |
| 统一天气场 / 气候代理 | 重建 | 由 seed + 时钟派生 |
| chunk 天气基线 | 重建 | chunk 注册时由气候模板与基线派生 |
| 区域跟踪器 / 季节去重标记 | 重建 | 每帧从场与时钟重算 |
| 区块 / 事件归档 | 各自通道 | chunks.db / events.db |

可重算量**不得**落盘：多存会让"删缓存由状态重算"失去检验意义，
也会把解析量固化成第二个事实源。

## 2. 状态载荷

`state.json.enc`（加密 + HMAC，见[存档系统](../../世界框架/存档系统/技术.md)）：

```json
{
  "state_version": 3,
  "clock":   {"time": 0, "speed": 1.0, "paused": false},
  "player":  {"entity_id": null, "x": 0.0, "y": 0.0},
  "weather": {
    "interventions": {"plan": [ <计划视图> ], "records": [ <记录视图> ]}
  },
  "archive_max_timestamp": 0
}
```

- **干预记录**只含**生效**记录（同键后到覆盖先到的胜者、未被清除者）。
  被替换的历史不落盘——它是研究溯源对象（P3 trace / 研究 API），不是
  $\mathcal W_t$。记录携带 `seq` / `applied_at`，读档回填原值，
  研究溯源不会因读档而重编号。
- **注入核不作为状态**（#51 / WC-6.5）：它是外部输入（`field_feature`
  计划）的时间线**投影**；计划的 `value` 携带完整核规格（中心/半径/
  强度/移动/出生/时长），读档按当前帧生效计划整体重建——保留为工具/
  测试路径的低层 `inject_core` 直连场景仍可用 `persist_injected` /
  `restore_injected`，但引擎状态载荷只认投影。

### 2.1 版本与 fail-closed

`STATE_VERSION = 3`（v3：注入核时间线投影），**无向后兼容**：读档在
任何副作用之前校验版本，缺失（旧格式）、非整数或与当前不符一律
`ValueError`。项目约定是"不承诺向后兼容、重构优先"，因此不写逐版本
迁移——格式变更即旧档不可读，错误信息明确区分"旧格式"与"未来格式"。

恢复路径逐条重新校验，**存档不是绕过校验的后门**：

- 干预计划/记录重新走执行前校验（目标已声明且已接线、值属声明值域、
  帧/时长合法、权限允许、实例匹配、参数标环境变化）；
- `field_feature` 计划额外校验：`active=True` 必须携带完整核规格，
  数值字段必须有限、`born_tick` 非负、`duration` 非负或 None；
  投影时特征类型必须已注册（未注册即拒绝）；
- 时间线恢复为**整体替换**（`restore(replace=True)`）：读档以载荷为
  唯一事实源，目标世界的残留计划/记录不得存活；
- 任一条非法 → **整体拒绝**，不留下半成品表或半成品核集合。

### 2.2 实例存在性：读档期的按需装载

干预目标实例（chunk）可能已被 LRU 淘汰而不在加载集内——这不是存档损坏。
`InterventionTable.restore(payload, instance_loader=...)` 接受一个装载器：
实例不存在时先尝试把它拉回来，仍不可用才拒绝。装载器**只在本次恢复调用
期间生效**，运行期登记不做隐式加载（避免"登记时偷偷加载 chunk"的副作用）。

世界接线方（`GameEngine._ensure_intervention_instance`）负责实现它：
从 `chunk_store` 取或生成目标 chunk，经 chunk 服务注册器注册（天气 +
地形状态引擎同源），tile 就绪时广播结算。

## 3. 世界设置（manifest）

存档位介绍信息（明文）记录定义这个世界的全部输入：

| 字段 | 内容 | 来源 |
| --- | --- | --- |
| `seed` | 世界种子 | 创建时定案 |
| `gen_params` | 世界生成调参（陆地比例、尺寸） | 创建世界流程 |
| `mechanism_declaration` | 声明 ID + 全量摘要 + 观测协议版本 | `MechanismRegistry.declaration_settings()` |

读档时 `validate_world_settings` 与当前注册表比对：

- **一致** → 放行；
- **不一致** → 拒绝加载。用新公式继续跑旧状态会得到一条"合法但不属于
  任何已声明世界"的轨迹，属于静默错误，必须挡住；
- **未记录**（P4 之前的存档）→ 放行，并由 `_persist_manifest` 立即补写，
  使下一次加载有可比对的事实。

## 4. 验收（W4）

04 §3.5 要求：从检查点复制 $\mathcal W_t$、清空可重建缓存、在相同未来
输入与随机地址下继续运行，两条轨迹一致。P4 的等价断言在
`tests/unit/test_save_completeness.py`：

1. **往返**：`collect_state` → JSON → `apply_state` → 再采集，逐字段相同
   （干预时间线等价；注入核经投影重建，规格逐字段一致）；
2. **双跑一致**：世界 A 施加干预 + 注入核后存档，世界 C 读档，世界 B
   完全不存档——B 与 C 在后续帧的逐 chunk 天气读出必须逐位一致；
3. **判别力**：剔除节点干预 / 剔除特征核计划（时间线投影）/ 两者全剔，
   必须各自**分叉**。没有这条负例，双跑一致可能只是"两边都没生效"的
   假通过。（实现注记 #50：单因子三臂；W4 采样窗口取注入核出生之后
   的帧，否则"缺核"无判别力。）

另有两层防护：恢复是**整体替换**（时间线 `replace=True` + 投影清空重建，
目标世界的残留计划与注入核不得存活），干预的 `seq` / `applied_at`
往返不变（研究溯源不可重编号）。

### 4.1 保存脉搏：帧边界捕获与整体提交（#51）

- **捕获**：tick 线程在帧边界调用 `GameEngine._capture_pulse()`——
  `collect_state`（时钟/玩家/干预时间线）与
  `ChunkStore.capture_pending()`（chunk 载荷）在**同一时刻**采集，
  载荷内部同属一个一致时刻；零 I/O；
- **提交**（保存线程）：事件 flush → state 写入 → chunk 提交 → manifest，
  **任一步失败即失败**（不静默部分成功）：失败计数与最近错误可查询
  （`save_pulse_failures` / `last_pulse_error`），未提交内容保留（chunk
  脏标记不清），下一次脉搏重试；
- **提交顺序**：state 先于 chunk——中途失败得到"state 新于 chunk"的
  可恢复组合（读档按积分游标补齐），而非 chunk 超前于 state 的隐藏状态
  （其后果无法由 W_t 重算）；
- **chunk 提交的 revision 纪律**：`ChunkData.mark_modified()`（dirty +
  版本）记录内容版本；提交时仅清除版本未变者的脏标记，捕获后被修改的
  chunk 留给下一次脉搏（`test_chunk_store.py::TestFrameBoundaryCapture`）；
- **世界失效**（WC-9.2）：帧事务提交相位失败 → 调度器标记 → 游戏层
  停表、停止保存、`world_invalidated` 可查询。

> **边界**：跨文件（state ↔ chunks.db）的绝对原子仍靠"state 先写 +
> 游标补齐"的可恢复组合保证，未引入跨文件代际标记。注入核已改为
> **时间线投影**（WC-6.5 / #51）——不再作为状态入档。

集成层（`tests/integration/test_game_engine.py`）走真实路径验证：进程 A
施加干预与强制特征核 → 保存脉搏落盘（state + manifest）→ 进程 B
`load_world` 后两者完整回来；世界设置不一致时拒绝加载。

## 5. 边界与遗留

- **W5 观测隔离未实现**：当前没有智能体观测主体（实体管理器"视野=全部"，
  事件桥接面向渲染），`AccessPolicy.observation_protocols` 已声明但无消费方。
  观测映射层与 W5 验收推迟到 P5（统一验收 runner）；届时 $G^i$ 只能从声明
  派生，研究日志不得成为智能体后门。**P3 研究 trace 同样推迟**——
  它服务于"逐节点对账"，而 W4 的双跑诊断在 P5 runner 里才需要它。
- **实体表全量持久化（issue #25）**：仍只有玩家位置随状态保存。
- **快照格式**：`.ascendsave` v2 不动，本交付只改其内的 state 载荷。
- **未存档的运行时状态**：天气引擎的 `_last_season`（事件去重标记）与
  区域跟踪器的上一帧区域集合按"可由 $\mathcal W_t$ 重算"处置，不入载荷；
  若将来某个标记影响未来轨迹（例如依赖历史做迟滞判定），必须改为分量。

## 6. 验证

- 单元：`test_save_completeness.py`（往返 / 投影规格保真 / 双跑 / 单因子
  判别力负例 / 读档 fail-closed）、`test_save_settings.py`（世界设置比对与
  声明视图负例）、`test_save_serializer.py`（版本与字段熔断）、
  `test_intervention.py::TestPersistRestore`（干预表往返、装载器语义、
  整体拒绝、`field_feature` 核规格校验）、
  `test_weather.py::TestFeatureField`（低层注入核往返与负例，工具路径）。
- 集成（`test_game_engine.py`，真实进程路径）：完整状态往返（干预表 +
  注入核投影 + manifest 世界设置）、声明不匹配拒绝加载、读档时钟先于
  区块结算就位（回归）、LRU 淘汰的干预目标可装载且 tile 就绪（回归）。
- 全链：pytest 全量（单元 + 集成）、`export_registry --check`、
  `gen_lean --check`、`verify_equations`、`graph_check`、Lean `lake build`。


## 7. 身份变更与旧档（#53 P4 迁移口径）

数值语义迁移（定点内核、冻表、模数声明）改变了世界身份的全部组成：
方程版本（实现内容变更）、`edges[*]` 的模数字段、数值内核源码依赖
（`ascend/num/*` 进身份摘要）。按 WC-1.2 与"不承诺向后兼容"约定：

- 迁移前创建的存档**按既有 fail-closed 路径拒绝加载**（manifest 声明/
  程序身份不一致），不提供迁移或兼容层；
- 需要保留的旧世界请以对应代码版本运行；跨版本比较必须显式声明世界族
  与配对方式（WC-1.4）；
- 本口径随 #53 P4 定稿，后续任何改变身份的批次沿用同一规则。
